<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SVGLClubConfigHelper.Pages.SettingsContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SVGLClubConfigHelper.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title Row -->
        <TextBlock 
            Grid.Row="0"
            Text="Settings"
            Style="{StaticResource SVGLPageTitleStyle}"/>

        <!-- Content Folder Settings Row -->
        <Grid
            Grid.Row="1"
            Margin="20">

            <StackPanel
                Spacing="8">

                <TextBlock
                    Text="AC Content Folder Location"
                    Style="{StaticResource SubtitleTextBlockStyle}" />

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="0.8*" />
                        <ColumnDefinition Width="0.2*" />
                    </Grid.ColumnDefinitions>

                    <TextBox
                        Text="{x:Bind ViewModel.ContentFolderPath, Mode=TwoWay}"
                        IsReadOnly="True"
                        Grid.Column="0" 
                        Margin="0, 0, 8, 0"/>

                    <Button
                        Content="Browse"
                        Grid.Column="1" 
                        Command="{x:Bind ViewModel.BrowseContentFolderCommand}"/>
                </Grid>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
