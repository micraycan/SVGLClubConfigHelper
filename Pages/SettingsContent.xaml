<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SVGLClubConfigHelper.Pages.SettingsContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SVGLClubConfigHelper.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title Row -->
        <TextBlock 
            Grid.Row="0"
            Text="Settings"
            Style="{StaticResource SVGLPageTitleStyle}"/>

        <!-- Content Folder Settings Row -->
        <Grid
            Grid.Row="1"
            Margin="20">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>


                <TextBlock
                    Grid.Row="0"
                    Text="AC Content Folder Location"
                    Style="{StaticResource SubtitleTextBlockStyle}" />

                <Grid
                    Grid.Row="1"
                    Margin="0, 6, 0, 0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="0.8*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBox
                        Grid.Column="0" 
                        Text="{x:Bind ViewModel.ContentFolderPath, Mode=TwoWay}"
                        IsReadOnly="True"
                        Margin="0, 0, 8, 0"/>

                    <Button
                        Grid.Column="1" 
                        Content="Browse"
                        Width="100"
                        Margin="0, 0, 8, 0"
                        Command="{x:Bind ViewModel.BrowseContentFolderCommand}"/>

                    <Button
                        Grid.Column="2"
                        IsEnabled="{x:Bind ViewModel.CanLoadContent, Mode=OneWay}"
                        Content="Load"
                        Width="100"
                        Command="{x:Bind ViewModel.LoadContentFolderCommand}"/>
                </Grid>

                <Grid
                    Grid.Row="2"
                    Margin="0, 6, 0, 0">

                    <TextBlock
                        Text="{x:Bind ViewModel.LastLoadDate, Mode=OneWay}" />
                </Grid>
                
            </Grid>
            
        </Grid>

    </Grid>
</Page>
